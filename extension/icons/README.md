# Icons

Placeholder icons (16/48/128px) are generated locally rather than checked
into git. Regenerate them anytime with:

```bash
pip install pillow --break-system-packages
python3 generate_icons.py
```
